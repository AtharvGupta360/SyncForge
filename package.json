{
  "name": "syncforge",
  "version": "0.1.0",
  "private": true,
  "description": "AI-powered collaborative code editor",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "tsc --build",
    "clean": "tsc --build --clean",
    "typecheck": "tsc --build --force"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
